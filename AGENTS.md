Technology Stack:

. Use SQLite for database.

Functional Requirements:

. Create a section on UI to show historical research output from all the user queries.
. Name this historical section as "Research Catalog"
. Save all the research output into a database so that it can be feteched later. 
. Historical research should a list in a tablular manner.
. Each record in the table should have column for a 1. sequence number, 2. date & timestamp 3. "user query", 4. "research output"
. Truncate "user query" to 50 characters.
. Truncate "research output" to 50 characters.
. Order the output by date & timestamp with most recent one on top.
. Make the "user query" and "research output" clickable. Upon clicking both, it should direct to the full research output.
. The historical output can be visible to all users
. Make the historical section "Research Catalog" look and feel like rest of the landing page.
